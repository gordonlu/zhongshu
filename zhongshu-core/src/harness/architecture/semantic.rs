use crate::harness::action::HarnessFeedback;

pub fn check_semantics(_content: &str, _file: &std::path::Path) -> Vec<HarnessFeedback> {
    Vec::new() // placeholder — full implementation in follow-up
}
