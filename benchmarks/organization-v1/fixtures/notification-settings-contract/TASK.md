# Notification settings contract

Reconcile the backend API contract, React settings panel, and product copy.
The final result must use one field name end to end and must not promise a
notification channel that the backend does not support.

This fixture is intentionally cross-domain. No single file contains the whole
problem, so backend, frontend, and writing responsibilities have measurable,
non-overlapping contributions.
